{
  "name": "uno",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev:api": "yarn workspace @uno/api dev",
    "dev:web": "yarn workspace @uno/web dev",
    "test:api": "yarn workspace @uno/api test",
    "build:api": "yarn workspace @uno/api build",
    "build:web": "yarn workspace @uno/web build"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
